
{% extends 'MarcaHomeBundle::layout.html.twig' %}

{% block content %}
<div class="row-fluid">
   <div class="span4">
      <a class="btn btn-primary" href="{{ path('page_new') }}">New</a>
   </div>
</div> 

<div class="row-fluid">
<div class="span6">
<table class="table table-bordered table-condensed table-striped">
<thead>
    <tr>        
        <th>Id</th>        
        <th>Body</th>        
        <th>Actions</th>
    </tr>
</thead>    
<tbody>
    {% for entity in entities %}
    <tr>        
        <td><a href="{{ path('page_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>        
        <td>{{ entity.body }}</td>        
        <td><div class="btn-group"><a class="btn" href="{{ path('page_show', { 'id': entity.id }) }}">show</a><a class="btn" href="{{ path('page_edit', { 'id': entity.id }) }}">edit</a></div>

</td>       
    </tr>
    
    {% endfor %}
    </tbody>
</table>
</div>
</div>
{% endblock %}
