{# Marca/HomeBundle/Resources/views/Page/show.html.twig #}

{% extends 'MarcaDocBundle::layout.html.twig' %}

{% block content %}
<div class="col-sm-11 center">
    {# Doc nav #}
    <nav class="navbar  navbar-inverse navbar-fixed-top" role="navigation">
        <div class="navbar-header col-sm-4 col-sm-4">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a name="top"></a>
            <a class="navbar-brand" href="#" title="{{page.title}}">{{page.title}}</a>
        </div>

        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav pull-right">
                <li id="skip"><a href="#main_content">Skip Navigation</a></li>
                <li><a href="{{ path('page_edit', { 'id': page.id }) }}">Edit</a>

                </li>
                <li><a href="{{ path('page') }}">Close</a>
                </li>

            </ul>
        </div>
    </nav>

    <div class="col-sm-11 well center">
    {{ page.body|raw }}
    </div>


{% endblock %}
