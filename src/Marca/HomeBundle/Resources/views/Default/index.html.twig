{# Marca/HomeBundle/Resources/views/Default/index.html.twig #}

{% extends 'MarcaHomeBundle:Default:layout.html.twig' %}
 
{% block body %}
    <div class="col-sm-6 col-xs-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h2>Welcome to {{ application_name }}</h2>
            </div>
            <div class="panel-body">

                <h3>Intuitive peer editing</h3>
                {{ application_name }} makes peer editing a breeze. Students can easily share their work with each other, offer comments, and provide detailed feedback using custom tags created by their instructors.

                <h3>Simply beautiful portfolios</h3>
                <p>Using {{ application_name }}, your students can create beautiful portfolios that combine text and rich multimedia.</p>

                <h3>Powerful, flexible markup</h3>
                <p>Whether you’re leaving initial feedback on a rough draft or thoroughly grading a final project, {{ application_name }} provides instructors with simple yet powerful and customizable tools for marking up documents. {{ application_name }} will help you grade more effectively and efficiently.</p>

            </div>
        </div>
    </div>

    <div class="col-sm-6 col-xs-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h2>Get started with {{ application_name }}</h2>
            </div>
            <div class="panel-body">
                <p>
                    {% if ldap_auth!='yes' %}
                        <a class="btn btn-primary" title="Get an Account" href="{{ path('fos_user_registration_register') }}" >Get an Account</a>
                    {% endif %}
                    <a class="btn btn-primary" title="Get an Account" href="{{ path('fos_user_security_login') }}" >Login</a>
                </p>
            </div>
        </div>
    </div>

    {% for page in pages %}
    <div class="col-sm-6 col-xs-12">
        <div class="panel panel-default">
            <div class="panel-heading">
            </div>
            <div class="panel-body">
                    {{ page.body|raw }}
            </div>
        </div>
    </div>
    {% endfor %}

{% endblock %}