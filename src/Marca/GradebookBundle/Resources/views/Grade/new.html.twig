{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Grade creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('grade') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
