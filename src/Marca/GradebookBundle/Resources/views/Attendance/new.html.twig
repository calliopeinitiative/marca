{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Attendance creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('attendance') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
