{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Gradeset creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('gradeset') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
