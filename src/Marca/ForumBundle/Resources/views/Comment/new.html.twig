{# Marca/ForumBundle/Resources/views/Comment/new.html.twig #}

{% extends 'MarcaForumBundle::layout.html.twig' %}

{% block content %}

<div class="span2">
<a class="btn btn-info" href="{{ course_path('comment') }}">List</a>
 
</div>       
  


<div class="span6 well"> 
<form action="{{ course_path('comment_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <div>
        <button class="btn" type="submit">Create</button>
    </div>
</form>
</div>
{% endblock %}
