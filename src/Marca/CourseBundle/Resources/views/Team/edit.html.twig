{# Marca/CourseBundle/Resources/views/Team/edit.html.twig #}


{% extends 'MarcaUserBundle::layout.html.twig' %}

{% block content %}
<div class="span4">
<legend>Teams</legend>   
<form action="{{ course_path('team_update', { 'id': team.id }) }}" method="post" {{ form_enctype(edit_form) }} novalidate="novalidate">
    {{ form_errors(edit_form) }}   
        
        {{ form_row (edit_form.name) }}      
        {{ form_row (edit_form.description) }}
        {{ form_row (edit_form.rolls) }}
        
    {{ form_rest(edit_form) }}     
<button class="btn btn-primary" type="submit">Update</button>
<a class="btn" href="{{ path('course_show', { 'courseid': courseid }) }}">Cancel</a></td></tr>          
</form>
</div>
{% endblock %}