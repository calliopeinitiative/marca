{# Marca/NoteBundle/Resources/views/Note/new.html.twig #}

{% extends 'MarcaHomeBundle::dialog.html.twig' %}

{% block javascripts %}
{% endblock %}

{% block content %}
<div class="col-sm-11 center">

<form action="{{ course_path('note_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <div>
        <button class="btn btn-primary" type="submit">Create</button><a class="btn btn-default" href="{{ course_path('note') }}">Cancel</a>
    </div>
</form>
</div>
{% endblock %}
