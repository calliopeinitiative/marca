{# Marca/AssignmentBundle/Resources/views/ReviewRubric/show.html.twig #}

{% extends 'MarcaAssignmentBundle::layout.html.twig' %}



{% block content %}
<h1>ReviewRubric</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ entity.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ entity.description }}</td>
        </tr>
        <tr>
            <th>Instructions</th>
            <td>{{ entity.instructions }}</td>
        </tr>
        <tr>
            <th>Keywords</th>
            <td>{{ entity.keywords }}</td>
        </tr>
        <tr>
            <th>Shared</th>
            <td>{{ entity.shared }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('reviewrubric') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('reviewrubric_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('reviewrubric_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
{% endblock %}