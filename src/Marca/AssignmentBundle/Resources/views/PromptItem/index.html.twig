<h1>PromptItem list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Prompt</th>
            <th>Helptext</th>
            <th>Type</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('promptitem_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.prompt }}</td>
            <td>{{ entity.helpText }}</td>
            <td>{{ entity.type }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('promptitem_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('promptitem_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('promptitem_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
