{# Marca/AssignmentBundle/Resources/views/ReviewRubric/new.html.twig #}

{% extends 'MarcaAssignmentBundle::layout.html.twig' %}

{% block content %}
<h1>ReviewResponse creation</h1>

<form action="{{ path('reviewresponse_create') }}" method="post" {{ form_start(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('reviewresponse') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}