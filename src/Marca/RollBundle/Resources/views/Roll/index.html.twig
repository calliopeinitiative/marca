
{% extends 'MarcaRollBundle:Roll:layout.html.twig' %}

{% block body %}

<div class="row-fluid">
   <div class="span12">
      <a class="btn btn-primary" href="{{ path('roll_new') }}">New</a>
   </div>
</div> 

<div class="row-fluid">
<div class="span12">
<table class="table table-bordered">
<tbody>
    {% for entity in entities %}
        <tr>            <td><a href="{{ path('roll_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>            <td>{{ entity.userid }}</td>            <td>{{ entity.courseid }}</td>            <td>{{ entity.role }}</td>            <td>{{ entity.status }}</td>            <td><div class="span12">     <a class="btn" href="{{ path('roll_show', { 'id': entity.id }) }}">show</a>     <a class="btn" href="{{ path('roll_edit', { 'id': entity.id }) }}">edit</a></div>  
            </td>        </tr>
    {% endfor %}
    </tbody>
</table>

{% endblock %}
