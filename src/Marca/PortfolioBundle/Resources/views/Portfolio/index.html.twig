
{% extends 'MarcaPortfolioBundle::layout.html.twig' %}

{% block content %}
<div class="row-fluid">
   <div class="span4">
      <a class="btn btn-primary" href="{{ course_path('portfolio_new') }}">New</a>
   </div>
</div> 

<div class="row-fluid">
<div class="span6">
<table class="table table-bordered table-condensed table-striped">
<thead>
    <tr>        
        <th>Id</th>        
        <th>Userid</th>        
        <th>Courseid</th>        
        <th>Fileid</th>        
        <th>Portorder</th>        
        <th>Actions</th>
    </tr>
</thead>    
<tbody>
    {% for portfolio in portfolios %}
    <tr>        
        <td><a href="{{ course_path('portfolio_show', { 'id': portfolio.id }) }}">{{ portfolio.id }}</a></td>        
        <td>{{ portfolio.userid }}</td>        
        <td>{{ portfolio.courseid }}</td>        
        <td>{{ portfolio.fileid }}</td>        
        <td>{{ portfolio.portOrder }}</td>        
        <td><div class="btn-group"><a class="btn" href="{{ course_path('portfolio_show', { 'id': portfolio.id }) }}">show</a><a class="btn" href="{{ course_path('portfolio_edit', { 'id': portfolio.id }) }}">edit</a></div>

</td>       
    </tr>
    
    {% endfor %}
    </tbody>
</table>
</div>
</div>
{% endblock %}
