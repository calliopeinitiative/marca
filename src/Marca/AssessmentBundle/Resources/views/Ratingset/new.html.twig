
{% extends 'MarcaYourBundle::layout.html.twig' %}

{% block javascripts %}
{% endblock %}

{% block content %}

<div class="col-md-2">
<a class="btn btn-info" href="{{ path('ratingset') }}">List</a>
</div>       
  
<div class="span9 well">
<form action="{{ path('ratingset_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <div>
        <button class="btn" type="submit">Create</button><a class="btn" href="">Cancel</a>
    </div>
</form>
</div>
{% endblock %}
