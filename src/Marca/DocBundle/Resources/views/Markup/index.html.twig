{% extends 'MarcaUserBundle::layout.html.twig' %}

{% block content %}
<div class="row-fluid">
   <div class="span4">
      <a class="btn btn-primary" href="{{ path('markup_new') }}">New</a>
   </div>
</div> 

<div class="row-fluid">
<div class="span6">
<table class="table table-bordered table-condensed table-striped">
<thead>
    <tr>        
        <th>Id</th>        
        <th>Userid</th>        
        <th>Name</th>        
        <th>Function</th>        
        <th>Value</th>        
        <th>Set</th>        
        <th>Actions</th>
    </tr>
</thead>    
<tbody>
    {% for entity in entities %}
    <tr>        
        <td><a href="{{ path('markup_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>        
        <td>{{ entity.userid }}</td>        
        <td>{{ entity.name }}</td>        
        <td>{{ entity.function }}</td>        
        <td>{{ entity.value }}</td>        
        <td>{{ entity.set }}</td>        
        <td><div class="btn-group"><a class="btn" href="{{ path('markup_show', { 'id': entity.id }) }}">show</a><a class="btn" href="{{ path('markup_edit', { 'id': entity.id }) }}">edit</a></div>

</td>       
    </tr>
    
    {% endfor %}
    </tbody>
</table>
</div>
</div>
{% endblock %}
